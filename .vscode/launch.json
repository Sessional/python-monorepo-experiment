{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug: App", // TODO: smarter way of finding which project to run?
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app:app",
                "--reload"
            ],
            "jinja": true,
            "cwd": "${workspaceFolder}/app"
        }
    ]
}